#include <cstdio>

const int maxn = 100005;
int A[maxn];
int N;

int main() {
    scanf("%d", &N);
    for (int i = 1; i <= N; ++i) scanf("%d", &A[i]);
    int ans = 0, p = 1;
    
    printf("%d\n", ans);
    return 0;
}
